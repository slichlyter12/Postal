Container{ //Node in the file map or sub container of file
    int id;
    int level; //0 == top == most zoomed out, 1 == next level down, 2 ...
    string name; //file name with extension or subFileStruct name. 
    string type; //file type
    
    int subContainers[]; links to containers of level > 0
    int links[]; //links to other file containers with level = 0
    int errors[]; //links to errors by id?
}

ErrorStruct{
    id;
    lineNumer;
    fileName;
    Error message;
}
